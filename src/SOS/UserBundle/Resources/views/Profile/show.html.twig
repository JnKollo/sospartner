{% extends 'content.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block content %}
    <div id="page-wrapper">
        <div class="row">
            {% for flash_message in app.session.flashBag.get('notice') %}
                <div class="alert alert-success">
                    {{ flash_message }}
                </div>
            {% endfor %}
            <div class="col-lg-12">
                <h1 class="page-header">Bienvenue {{ app.user.username|capitalize }}</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        Actualités sportives
                    </div>
                    <div class="panel-body">
                        <div class="row">
                            <div class="col-lg-6">
                                <div class="panel panel-default">
                                    <div class="panel-heading">
                                        {{ xml.channel.title }}
                                    </div>
                                    <div class="panel-body">
                                    </div>
                                    <!-- /.panel-body -->
                                </div>
                                <!-- /.panel -->
                            </div>
                            <!-- /.col-lg-6 -->
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}